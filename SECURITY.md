# Security Policy / 安全策略

## Supported versions

PromptSoul 仍处于实验阶段。安全修复只面向 `main` 和最新公开版本；旧快照不提供维护承诺。

## Security model

PromptSoul 是本机优先、自托管的开发原型，服务默认只绑定 `127.0.0.1`。当前没有账号系统、租户隔离、公网身份验证、速率限制或面向互联网的安全边界。请勿把开发服务直接暴露到公网。

重点安全边界包括：

- API Key 只能保存在 Node 进程内存中，不得进入浏览器存储、文件、日志或响应；
- Provider 设置、动作生成和动作删除只接受本机回环地址上的同源请求；
- 模型 ZIP 必须抵御路径穿越、符号链接、特殊文件、压缩炸弹和越界写入；
- AI 输出只能作为受限 JSON 数据处理，不能执行代码、命令或任意路径；
- 生成或删除动作只能影响活动模型的 `PromptSoul` 组，不能修改模型原有组；
- 受限 Live2D 模型和私有素材不能进入仓库、漏洞报告或公开日志。

## Reporting a vulnerability

优先使用 GitHub 仓库 **Security → Report a vulnerability** 的私密漏洞报告功能。若该功能尚未启用，请从 New Issue 选择 **Private contact request** 模板；该公开请求中只能说明需要私下联系，不能填写漏洞或行为事件细节。

报告中请包含受影响版本、最小复现步骤、影响和建议修复，但不要附上真实 API Key、个人聊天内容、私有模型、授权受限素材或可直接滥用的公开利用代码。维护者确认收到后会在私密渠道协调验证、修复和披露。

依赖漏洞也可按同一渠道报告。请勿仅凭自动扫描结果公开一个未经验证的高危结论。
