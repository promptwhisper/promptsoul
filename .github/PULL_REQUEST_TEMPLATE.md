## 变更说明

说明要解决的问题、采用的方案，以及为什么需要这个变更。

## 验证

列出实际运行的命令、浏览器尺寸和人工检查结果。

## Checklist

- [ ] 变更范围聚焦，并补充了必要的测试和文档。
- [ ] 已运行 `npm run verify` 和 `git diff --check`。
- [ ] 没有提交 API Key、`.env`、聊天记录、私有提示词、模型、纹理、生成动作或本地配置。
- [ ] 没有把运行时 Key 写入浏览器存储、文件、日志或响应。
- [ ] 动作只写入 `PromptSoul`，未修改 `Action`、`Idle`、`Tap` 等原有组。
- [ ] 新模型先运行了 `npm run analyze:model`，没有照搬 Hiyori 参数。
- [ ] 视觉变更已检查桌面与 390×844 手机端，无横向溢出且输入字号至少 16px。
- [ ] 包含 Hiyori 的截图在画面内保留 `Hiyori Momose ©Live2D`。
- [ ] 已确认新增依赖、代码和素材的许可证允许本仓库使用与分发。

## 关联 Issue

<!-- 例如：Closes #123 -->
