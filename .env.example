# Non-secret defaults only. Never put a real API Key in this file.
# Export NPC_API_KEY in your shell or enter a temporary Key in the local UI.
NPC_API_BASE=https://api.openai.com/v1
# Replace this with a model your configured Provider actually supports.
NPC_MODEL=gpt-5.6-luna
